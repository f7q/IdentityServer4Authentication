<Window x:Class="IdentityClient.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:IdentityClient"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <Button Content="Button" HorizontalAlignment="Left" Height="41" Margin="121,10,0,0" VerticalAlignment="Top" Width="74" Click="Button_Click"/>
        <TextBox x:Name="txt_box" HorizontalAlignment="Left" Height="62" Margin="121,67,0,0" TextWrapping="Wrap" Text="TextBox" VerticalAlignment="Top" Width="571" />
        <!--DataGrid HorizontalAlignment="Left" Height="237" Margin="121,145,0,0" VerticalAlignment="Top" Width="571">
            <DataGrid.Columns>
                <DataGridTextColumn Binding="{Binding Path=Key.Value, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}" Header="Key" />
                <DataGridTextColumn Binding="{Binding Path=Val.Value, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}" Header="Value" />
            </DataGrid.Columns>
        </DataGrid-->

    </Grid>
</Window>
